{
  "name": "artillery-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Artillery load testing",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.4.0",
    "zod": "^3.22.4",
    "pino": "^8.17.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "tsx": "^4.0.0",
    "vitest": "^1.0.0",
    "@vitest/coverage-v8": "^1.0.0"
  },
  "keywords": ["mcp", "artillery", "load-testing", "server"],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
} 
